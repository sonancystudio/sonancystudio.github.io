// Function to handle switching between subpages (tabs)function openTab(evt, tabName) {    let i, tabcontent, tablinks;    // Hide all tab contents    tabcontent = document.getElementsByClassName("tab-content");    for (i = 0; i < tabcontent.length; i++) {        tabcontent[i].style.display = "none";    }    // Remove the background color of all tablinks    tablinks = document.getElementsByClassName("tab-link");    for (i = 0; i < tablinks.length; i++) {        tablinks[i].className = tablinks[i].className.replace(" active", "");    }    // Show the current tab, and add an "active" class to the button that opened the tab    document.getElementById(tabName).style.display = "block";    evt.currentTarget.className += " active";}// Function to parse Markdown to HTML on page loaddocument.addEventListener("DOMContentLoaded", function() {    // Find all elements containing markdown source    const markdownSections = document.querySelectorAll('.tab-content');        markdownSections.forEach(section => {        const sourceDiv = section.querySelector('.markdown-source');        const outputDiv = section.querySelector('.markdown-output');                if (sourceDiv && outputDiv) {            // Get the raw text, trim it, and parse it using marked.js            const rawMarkdown = sourceDiv.innerHTML.trim();            outputDiv.innerHTML = marked.parse(rawMarkdown);        }    });});